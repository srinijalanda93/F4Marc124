import React from 'react';
import ReactDOM from 'react-dom/client';
import './index.css';


const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
 <>
 <p>This the paragraph tag inside index.js</p>
 </>
);

console.log("hello it's working");
